# Backend API URL (optional, defaults to http://localhost:3001)
VITE_API_URL=http://localhost:3001/api

# Backend server port (optional, defaults to 3001)
PORT=3001

# Admin Panel Password
# Set this to your desired admin password. This will be used as the initial password
# when the database is first created. If not set, you'll need to set a password
# through the admin panel after first login.
ADMIN_PASSWORD=your-secure-password-here

# Google Cloud Firestore Configuration
# Required for database operations. See FIRESTORE_SETUP.md for detailed setup instructions.
# Your Google Cloud Project ID where Firestore is enabled
GCP_PROJECT_ID=your-project-id
# Alternative: You can use GOOGLE_CLOUD_PROJECT instead of GCP_PROJECT_ID

# For local development with service account key file (optional):
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
# Note: In Cloud Run, authentication is handled automatically via the service account.

# FTP Storage Configuration (for file uploads)
# Required for file uploads to work. See FTP_SETUP.md for detailed instructions.
FTP_HOST=ftp.yourdomain.com
FTP_USER=your_ftp_username
FTP_PASSWORD=your_ftp_password
FTP_PORT=21
FTP_SECURE=false
FTP_BASE_PATH=/public_html/uploads
FTP_BASE_URL=https://yourdomain.com/uploads
